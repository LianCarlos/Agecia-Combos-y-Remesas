/**
 * Servicio de monedas (currencies).
 * Re-exporta desde currencies.ts para mantener compatibilidad.
 */
export {
  getActiveCurrencies as getActiveCountries,
  getAllCurrencies as getAllCountries,
} from "@/lib/services/currencies";

